{
  "name": "scrapline",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Scrapline — neon cyberpunk idle-tycoon for CrazyGames. Vite + TypeScript build.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.9.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^4.1.7"
  }
}
